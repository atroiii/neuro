esse é um arquivo com tudo que o frontend precisa saber sobre o projeto:
- Para adicionar novos filtros de dados EEG, siga os passos documentados no arquivo brainlink_bridge.dart.
- A classe EEGData em lib/data/models/eeg_data.dart contém os campos atuais para as bandas de ondas cerebrais.
- A comunicação com o código nativo é feita via MethodChannel 'com.brainlink.app/sdk'.
- Certifique-se de atualizar o factory constructor fromMap() na classe EEGData ao adicionar novos campos.
- O SDK nativo (Java) deve ser configurado para enviar os novos campos no Map.
- A Stream connectionStateStream em brainlink_bridge.dart permite monitorar o estado da conexão com o dispositivo EEG.
- Os nomes das ondas e suas grafias usadas sao : delta, theta, lowAlpha, highAlpha, lowBeta, highBeta, lowGamma, midGamma,e no codigo estao como tal
- A soma das ondas Gamma é acessível via o getter totalGamma na classe EEGData, e novos getters podem ser adicionados conforme necessário na mesma classe,para novas bandas de ondas cerebrais.
-Todo o código relevante para a manipulação dos dados EEG e comunicação com o dispositivo está localizado em lib/native/brainlink_bridge.dart e lib/data/models/eeg_data.dart.
- Mantenha a consistência dos nomes e tipos de dados entre o código Dart e o SDK nativo para garantir a correta transmissão e interpretação dos dados EEG.
-Todo o código foi feito relacionado a esses pontos foi feito por mim Gustavo Bezerra e é bem intuitivo.
